# dm_hook 代码审查标准与流程规范

> **版本**: 2.0  
> **制定日期**: 2026-08-11  
> **适用范围**: dm_hook 全部 C++ 代码（dm.cpp / dm.h / dllmain.cpp 及后续新增文件）  
> **审查专家**: CodeReviewExpert (C++ Code Review Master)

---

## 目录

- [第一章 代码审查标准](#第一章-代码审查标准)
  - [1.1 严重程度分级体系](#11-严重程度分级体系)
  - [1.2 六大质量评估维度](#12-六大质量评估维度)
  - [1.3 命名约定规范](#13-命名约定规范)
  - [1.4 代码规范细则](#14-代码规范细则)
  - [1.5 错误处理规范](#15-错误处理规范)
  - [1.6 安全性检查规范](#16-安全性检查规范)
  - [1.7 性能考量规范](#17-性能考量规范)
- [第二章 代码审查流程](#第二章-代码审查流程)
  - [2.1 全流程概览](#21-全流程概览)
  - [2.2 阶段一：提交前自检](#22-阶段一提交前自检)
  - [2.3 阶段二：同行评审](#23-阶段二同行评审)
  - [2.4 阶段三：合并与跟踪](#24-阶段三合并与跟踪)
  - [2.5 审查触发条件矩阵](#25-审查触发条件矩阵)
  - [2.6 审查时限与规模](#26-审查时限与规模)
  - [2.7 审查评论格式规范](#27-审查评论格式规范)
- [第三章 审查人角色与职责](#第三章-审查人角色与职责)
  - [3.1 角色定义](#31-角色定义)
  - [3.2 被审查人职责](#32-被审查人职责)
  - [3.3 审查人职责](#33-审查人职责)
  - [3.4 维护者职责](#34-维护者职责)
  - [3.5 职责边界与冲突处理](#35-职责边界与冲突处理)
  - [3.6 沟通方式与反馈规范](#36-沟通方式与反馈规范)
- [第四章 审查检查清单](#第四章-审查检查清单)
  - [4.1 通用检查清单](#41-通用检查清单)
  - [4.2 内存安全专项](#42-内存安全专项)
  - [4.3 类型安全与 64 位兼容专项](#43-类型安全与-64-位兼容专项)
  - [4.4 安全专项](#44-安全专项)
  - [4.5 线程安全专项](#45-线程安全专项)
  - [4.6 性能专项](#46-性能专项)
  - [4.7 现代 C++ 专项](#47-现代-c-专项)
  - [4.8 可维护性专项](#48-可维护性专项)
- [第五章 度量与持续改进](#第五章-度量与持续改进)
  - [5.1 核心度量指标](#51-核心度量指标)
  - [5.2 代码质量评分体系](#52-代码质量评分体系)
  - [5.3 持续改进机制](#53-持续改进机制)
  - [5.4 度量报告模板](#54-度量报告模板)
- [附录](#附录)
  - [附录 A: 审查报告模板](#附录-a-审查报告模板)
  - [附录 B: 开发者自查快速清单](#附录-b-开发者自查快速清单)
  - [附录 C: 自动化工具配置](#附录-c-自动化工具配置)
  - [附录 D: 已知问题清单](#附录-d-已知问题清单)
  - [附录 E: 术语表](#附录-e-术语表)

---

## 第一章 代码审查标准

### 1.1 严重程度分级体系

每个审查发现按严重程度分为四级，决定其处理优先级和合并门槛：

| 级别 | 标记 | 含义 | 合并影响 | 修复时限 | 示例 |
|------|------|------|----------|----------|------|
| **P0** | 🔴 阻塞项 (Blocker) | 存在安全漏洞、崩溃风险或数据损坏，必须修复才能合并 | **阻塞合并** | 立即修复 | 命令注入、空指针解引用、缓冲区溢出 |
| **P1** | 🟡 关键项 (Critical) | 存在内存泄漏、性能严重退化或逻辑错误，强烈建议本次修复 | 需限期修复 | 3 个工作日内 | 资源泄漏、异常路径不安全、忽略返回值 |
| **P2** | 🔵 建议项 (Suggestion) | 代码质量改进、可读性提升、最佳实践建议 | 不阻塞，记录 backlog | 当前迭代内 | 冗余拷贝、缺少注释、命名不规范 |
| **P3** | 💭 可选项 (Nit) | 风格偏好、微优化，不强制要求 | 不影响合并 | 无要求 | 空行数量、变量名风格偏好 |

**合并规则：**
- 存在任一 P0 项 → **禁止合并**
- P1 项超过 3 个 → **禁止合并**（需先修复至少到 3 个以内）
- P2/P3 项不阻塞合并，但需记录跟踪

### 1.2 六大质量评估维度

#### 维度 A：内存安全（权重 25%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| A1 | 🔴 P0 | 禁止裸 new/delete，必须使用 RAII | `new` 必须包裹在 `unique_ptr`/`shared_ptr` 中 |
| A2 | 🔴 P0 | 禁止缓冲区溢出 | `sprintf` → `snprintf`；`sscanf %s` → `%Ns` |
| A3 | 🔴 P0 | 禁止使用未初始化变量 | 所有局部变量声明时必须初始化 |
| A4 | 🟡 P1 | 所有 Windows Handle 必须用 RAII 管理 | HANDLE/HDC/HBITMAP 需自动释放 |
| A5 | 🟡 P1 | 跨进程内存操作必须检查返回值 | ReadProcessMemory/WriteProcessMemory |
| A6 | 🟡 P1 | GlobalLock 返回值必须检查 NULL | 防止空指针解引用 |
| A7 | 🔵 P2 | malloc/free 应替换为 new/delete 或容器 | C++ 中不应直接使用 C 内存管理 |

**正反示例：**

```cpp
// ❌ P0: 裸 new 无 RAII
Bitmap* bmp = new Bitmap(path);
// ... 如果后续代码抛异常，bmp 泄漏
delete bmp;

// ✅ 正确: RAII 管理
auto bmp = std::make_unique<Bitmap>(path);
// 自动释放，异常安全

// ❌ P0: sprintf 无长度限制
char buf[16];
sprintf(buf, "%02X%02X%02X", r, g, b);

// ✅ 正确: snprintf 限长
char buf[16];
snprintf(buf, sizeof(buf), "%02X%02X%02X", r, g, b);

// ❌ P1: GlobalLock 返回值未检查
memcpy(GlobalLock(hMem), value, len);

// ✅ 正确: 检查 NULL
void* p = GlobalLock(hMem);
if (!p) { CloseClipboard(); return 0; }
memcpy(p, value, len);
GlobalUnlock(hMem);
```

#### 维度 B：类型安全与 64 位兼容（权重 15%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| B1 | 🔴 P0 | 禁止 C 风格强制转换 | 使用 `static_cast`/`reinterpret_cast`/`const_cast` |
| B2 | 🔴 P0 | 禁止指针截断 | `(long)hwnd` 在 64 位下丢失高位 |
| B3 | 🟡 P1 | 使用 constexpr 替代 #define | 类型安全的编译期常量 |
| B4 | 🟡 P1 | 指针存储使用 intptr_t/uintptr_t | 而非 long/int |
| B5 | 🔵 P2 | 使用 scoped enum 替代魔法数字 | `enum class` 提供类型安全 |

**正反示例：**

```cpp
// ❌ P0: C 风格转换 + 指针截断
return (long)hwnd;           // 64 位下高位丢失！
HWND h = (HWND)hwnd;

// ✅ 正确: C++ 转换 + intptr_t
return static_cast<long>(reinterpret_cast<intptr_t>(hwnd));
HWND h = reinterpret_cast<HWND>(static_cast<intptr_t>(hwnd));

// ❌ P1: #define 常量
#define DM_MAX_PATH 260

// ✅ 正确: constexpr
constexpr int DM_MAX_PATH = 260;
```

#### 维度 C：安全性（权重 20%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| C1 | 🔴 P0 | 禁止命令注入 | 用户输入不得直接传入 _popen/system |
| C2 | 🔴 P0 | 禁止不安全字符串格式化 | sscanf 的 %s 必须限长 |
| C3 | 🔴 P0 | 禁止全局禁用安全警告 | 不使用 _CRT_SECURE_NO_WARNINGS |
| C4 | 🟡 P1 | Windows API 返回值必须检查 | 防止静默失败 |
| C5 | 🟡 P1 | 外部输入必须验证 | 路径、文件名需检查合法性 |
| C6 | 🔵 P2 | 敏感操作需权限检查 | 进程操作、注册表访问 |

#### 维度 D：性能（权重 15%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| D1 | 🟡 P1 | 禁止逐像素 GetDC/ReleaseDC | 批量截屏替代逐像素读取 |
| D2 | 🟡 P1 | 循环内禁止不必要的内存分配 | 提取到循环外 |
| D3 | 🔵 P2 | 避免不必要的字符串拷贝 | 使用 string_view 或引用 |
| D4 | 🔵 P2 | 找图算法应有提前退出机制 | 命中后立即返回 |
| D5 | 💭 P3 | 大数据操作考虑 SIMD/多线程 | SSE/AVX 加速像素比较 |

#### 维度 E：可维护性（权重 15%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| E1 | 🟡 P1 | 单文件不超过 1000 行 | 当前 dm.cpp 3366 行需拆分 |
| E2 | 🟡 P1 | 单函数不超过 100 行 | 超过需提取子函数 |
| E3 | 🔵 P2 | 使用命名空间隔离内部符号 | 避免全局命名空间污染 |
| E4 | 🔵 P2 | 桩函数需统一标记 | 区分已实现/未实现 |
| E5 | 🔵 P2 | 公共 API 需有文档注释 | 参数含义、返回值、错误码 |
| E6 | 💭 P3 | 文件头需有模块说明 | 模块职责、依赖关系 |

#### 维度 F：现代 C++ 实践（权重 10%）

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| F1 | 🟡 P1 | 使用 RAII 包装所有资源 | Handle/DC/Bitmap/CriticalSection |
| F2 | 🔵 P2 | 使用 string_view 替代 const char* + strlen | 减少不必要的 strlen 调用 |
| F3 | 🔵 P2 | 使用 [[nodiscard]] 标注不可忽略的返回值 | 防止忽略错误码 |
| F4 | 🔵 P2 | 使用 scoped enum 替代裸枚举/魔法数字 | enum class |
| F5 | 💭 P3 | 使用 std::optional 表达可能失败的返回值 | 替代哨兵值 |
| F6 | 💭 P3 | 使用 std::format 替代 sprintf (C++20) | 类型安全的格式化 |

### 1.3 命名约定规范

| 类别 | 规范 | 示例 | 级别 |
|------|------|------|------|
| 导出 API 函数 | `dm_` 前缀 + snake_case | `dm_findWindow`, `dm_getColor` | 🔴 P0 |
| 内部静态函数 | snake_case，无前缀 | `Utf8ToWide`, `ParseColor` | 🟡 P1 |
| 宏常量 | `DM_` 前缀 + UPPER_SNAKE | `DM_MAX_PATH`, `DMERR_SUCCESS` | 🟡 P1 |
| constexpr 常量 | `DM_` 前缀 + UPPER_SNAKE | `constexpr int DM_MAX_PATH = 260` | 🟡 P1 |
| 结构体/类 | PascalCase | `DmState`, `UniqueBitmap` | 🟡 P1 |
| 成员变量 | snake_case | `globalPath`, `lastError` | 🔵 P2 |
| 局部变量 | snake_case | `hdc_screen`, `result_str` | 🔵 P2 |
| 全局变量 | `g_` 前缀 + snake_case | `g_tlsIndex`, `g_nextObjectId` | 🟡 P1 |
| 类型别名 | `_t` 后缀或 PascalCase | `DmState`, `UniqueBitmap` | 🔵 P2 |

### 1.4 代码规范细则

| 编号 | 级别 | 规则 |
|------|------|------|
| S1 | 🔴 P0 | 缩进使用 4 个空格，禁止 Tab |
| S2 | 🔴 P0 | 每行不超过 120 字符 |
| S3 | 🟡 P1 | 大括号风格: K&R (开括号不换行) |
| S4 | 🟡 P1 | 指针/引用: `Type* name` 而非 `Type *name` |
| S5 | 🟡 P1 | include 顺序: 自有头 → C 标准库 → C++ 标准库 → Windows → 第三方 |
| S6 | 🔵 P2 | 函数间空一行 |
| S7 | 🔵 P2 | 头文件保护使用 `#pragma once` + `#ifndef` 双重保护 |
| S8 | 🔵 P2 | 注释使用 `//`，块注释使用 `/* */` 仅用于文件头和大段说明 |

### 1.5 错误处理规范

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| E1 | 🔴 P0 | 所有指针参数必须检查 nullptr | 防止空指针解引用 |
| E2 | 🟡 P1 | 所有 Windows API 返回值必须检查 | 防止静默失败 |
| E3 | 🟡 P1 | 使用 DM_SetLastError 设置错误码 | 调用方可通过 dm_getLastError 获取 |
| E4 | 🟡 P1 | 错误路径必须释放已获取的资源 | 使用 RAII 保证异常安全 |
| E5 | 🔵 P2 | 错误码需有对应文档说明 | 参数含义、返回值、错误码 |
| E6 | 🔵 P2 | 避免使用 errno | 使用平台无关的错误处理 |

**正反示例：**

```cpp
// ❌ P0: 未检查 nullptr
DM_API long dm_setPath(const char* path) {
    DmState* s = GetDmState();
    s->globalPath = Utf8ToWide(path);  // path 可能为 NULL
    return 1;
}

// ✅ 正确: 检查 nullptr
DM_API long dm_setPath(const char* path) {
    DmState* s = GetDmState();
    if (!s) { DM_SetLastError(DMERR_EXCEPTION); return 0; }
    if (!path || !*path) { DM_SetLastError(4); return 0; }
    s->globalPath = Utf8ToWide(path);
    DM_SetLastError(DMERR_SUCCESS);
    return 1;
}
```

### 1.6 安全性检查规范

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| SEC1 | 🔴 P0 | 用户输入不得直接传入命令执行函数 | _popen/system/CreateProcess 需验证输入 |
| SEC2 | 🔴 P0 | 文件路径需检查路径穿越 | 禁止 `../` 越权访问 |
| SEC3 | 🔴 P0 | 禁止使用 sprintf 系列无长度限制函数 | 使用 snprintf 或 std::format |
| SEC4 | 🟡 P1 | 进程操作需验证目标进程合法性 | 防止操作系统进程 |
| SEC5 | 🟡 P1 | 内存读写需验证地址范围合法性 | 防止越界访问 |
| SEC6 | 🔵 P2 | 敏感数据（密码/注册码）不应硬编码 | 使用配置文件或环境变量 |

### 1.7 性能考量规范

| 编号 | 级别 | 规则 | 说明 |
|------|------|------|------|
| P1 | 🟡 P1 | 禁止在循环内调用 GetDC/ReleaseDC | 批量截屏一次读取 |
| P2 | 🟡 P1 | 禁止在循环内分配/释放堆内存 | 提取到循环外 |
| P3 | 🔵 P2 | 字符串拼接使用 reserve 预分配 | 减少重新分配 |
| P4 | 🔵 P2 | 大循环考虑使用缓存友好的数据布局 | 结构体数组 vs 链表 |
| P5 | 💭 P3 | 热点函数考虑使用 SIMD 指令优化 | SSE/AVX 像素比较 |

---

## 第二章 代码审查流程

### 2.1 全流程概览

```
┌─────────────────────────────────────────────────────────────────┐
│                     代码审查全流程                                │
│                                                                   │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│  │ 开发完成  │───▶│ 提交前自检 │───▶│ 提交 PR  │───▶│ 同行评审  │  │
│  └──────────┘    └──────────┘    └──────────┘    └────┬─────┘  │
│                                                       │         │
│                       ┌───────────────────────────────┘         │
│                       ▼                                         │
│                 ┌──────────┐                                    │
│                 │ 审查结论  │                                    │
│                 └────┬─────┘                                    │
│              ┌───────┼───────┐                                  │
│              ▼       ▼       ▼                                  │
│         ┌──────┐ ┌──────┐ ┌──────────┐                          │
│         │Approve│ │Change│ │ Comment  │                          │
│         └──┬───┘ └──┬───┘ └────┬─────┘                          │
│            │        │          │                                 │
│            ▼        ▼          ▼                                 │
│      ┌─────────┐  修改代码   仅评论                              │
│      │  合并    │  ◀─────│    不阻塞                             │
│      └────┬────┘       │                                         │
│           ▼            ▼                                         │
│      ┌──────────┐  ┌──────────┐                                 │
│      │ 合并后跟踪│  │  重新提交  │─────────▶ 同行评审              │
│      └──────────┘  └──────────┘                                 │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 阶段一：提交前自检

**执行人**: 开发者（被审查人）  
**时机**: 代码编写完成，提交 PR/MR 之前

**自检步骤：**

| 步骤 | 内容 | 工具/方法 |
|------|------|-----------|
| 1 | 本地编译无 warning | `cmake --build . --config Release` 并启用 `/W4` |
| 2 | 静态分析通过 | `clang-tidy` + `cppcheck` |
| 3 | 代码格式化通过 | `clang-format` |
| 4 | 对照检查清单自查 | 参见 [第四章](#第四章-审查检查清单) |
| 5 | 编写 PR 描述 | 说明变更内容、影响范围、测试方式 |
| 6 | 关联 Issue | 如有对应 Issue 需关联 |

**PR 描述模板：**

```markdown
## 变更说明
[简要描述本次变更的内容和目的]

## 变更类型
- [ ] 新功能
- [ ] Bug 修复
- [ ] 重构
- [ ] 性能优化
- [ ] 文档更新

## 影响范围
[列出受影响的模块和功能]

## 测试方式
[描述如何测试本次变更]

## 自检清单
- [ ] 编译无 warning (/W4)
- [ ] 静态分析通过
- [ ] 无新增 sprintf → snprintf
- [ ] 无新增 C 风格强制转换
- [ ] 无新增裸 new/delete
- [ ] 所有指针参数检查 nullptr
- [ ] 所有 Windows API 返回值已检查
```

### 2.3 阶段二：同行评审

**执行人**: 审查人（1-2 名）  
**时机**: PR 提交后

**评审步骤：**

| 步骤 | 内容 | 时限 |
|------|------|------|
| 1 | 运行自动化预检（clang-tidy/cppcheck diff） | 自动触发 |
| 2 | 阅读 PR 描述，理解变更意图 | 15 分钟内 |
| 3 | 按 6 大维度逐项审查代码 | 见时限表 |
| 4 | 标注问题优先级（P0-P3） | 随审查同步 |
| 5 | 提出具体修改建议（含代码示例） | 随审查同步 |
| 6 | 给出审查结论 | 审查完成后 |

**审查结论类型：**

| 结论 | 含义 | 后续动作 |
|------|------|----------|
| **Approve** | 代码质量合格，可以合并 | 进入合并流程 |
| **Request Changes** | 存在 P0/P1 问题需修改 | 开发者修改后重新提交 |
| **Comment** | 有建议但不阻塞合并 | 开发者可选修改 |

### 2.4 阶段三：合并与跟踪

**执行人**: 维护者  
**时机**: 审查通过后

**合并步骤：**

| 步骤 | 内容 |
|------|------|
| 1 | 确认所有 P0 问题已修复 |
| 2 | 确认 P1 问题已修复或有明确计划 |
| 3 | 执行合并（Squash Merge 推荐） |
| 4 | 将 P2/P3 问题创建为 Issue 跟踪 |
| 5 | 更新质量趋势看板 |
| 6 | 如为定期审查，生成审查报告 |

### 2.5 审查触发条件矩阵

| 触发场景 | 审查范围 | 流程 | 审查人数量 |
|----------|----------|------|------------|
| 新功能开发 | 新增代码 + 影响范围 | 全流程三阶段 | 2 人 |
| Bug 修复 | 修复代码 + 回归测试 | 阶段一 + 二 | 1 人 |
| 重构 | 重构范围 + 接口兼容性 | 全流程三阶段 | 2 人 |
| 紧急修复 | 修复代码 | 阶段二快速审查，事后补全 | 1 人 |
| 定期审查 | 全量代码 | 阶段二 + 三 | 2 人 + 架构审查 |
| 文档更新 | 文档内容 | 阶段二（仅审查正确性） | 1 人 |

### 2.6 审查时限与规模

| PR 规模（变更行数） | 审查时限 | 审查人数量 | 附加要求 |
|---------------------|----------|------------|----------|
| < 100 行 | 4 小时内 | 1 人 | - |
| 100 - 500 行 | 1 个工作日内 | 1 人 | - |
| 500 - 2000 行 | 2 个工作日内 | 2 人 | 需架构审查 |
| > 2000 行 | 3 个工作日内 | 2 人 + 架构师 | 需拆分为多个 PR |

**单次审查上限**: 单个 PR 不宜超过 500 行变更。超过 500 行时，建议拆分为多个小 PR，以便更有效地审查。

### 2.7 审查评论格式规范

**标准评论格式：**

```
🔴 [P0] 问题标题: 简要描述
文件: dm.cpp:行号
函数: 函数名

问题:
[描述具体问题，包括代码上下文]

影响:
[说明该问题可能导致的后果]

建议:
[提供具体的修改建议，含代码示例]

参考:
[相关的 C++ Core Guidelines 条目或最佳实践文档]
```

**评论示例：**

```
🔴 [P0] 内存安全: sprintf 无长度限制
文件: dm.cpp:159
函数: ColorToBGRStr

问题:
char buf[16];
sprintf(buf, "%02X%02X%02X", GetBValue(c), GetGValue(c), GetRValue(c));
sprintf 不检查缓冲区大小，如果格式化结果超过 15 字节会导致缓冲区溢出。

影响:
缓冲区溢出可能导致程序崩溃或被利用执行任意代码。

建议:
char buf[16];
snprintf(buf, sizeof(buf), "%02X%02X%02X", GetBValue(c), GetGValue(c), GetRValue(c));

参考:
C++ Core Guidelines R.32, CERT STR31-C
```

**评论原则：**
- 对事不对人：评论针对代码，不针对开发者
- 提供方案：不仅指出问题，还要给出修改建议
- 解释原因：说明为什么这是问题，以及影响是什么
- 引用标准：引用相关的 C++ Core Guidelines 或 CERT 规则

---

## 第三章 审查人角色与职责

### 3.1 角色定义

| 角色 | 职责 | 资格要求 |
|------|------|----------|
| **开发者** (Author) | 编写代码、提交 PR、修复审查问题 | 熟悉项目代码规范 |
| **审查人** (Reviewer) | 审查代码、提出问题、给出结论 | 熟悉审查标准和 C++ 最佳实践 |
| **维护者** (Maintainer) | 合并代码、跟踪问题、维护质量 | 深入理解项目架构 |
| **架构师** (Architect) | 架构审查、重大决策 | 对项目整体架构有深入理解 |

### 3.2 开发者职责

| 职责 | 说明 |
|------|------|
| 代码自检 | 提交前对照检查清单逐项自查 |
| 编写 PR 描述 | 清晰描述变更内容、影响范围、测试方式 |
| 及时修复 | 在审查人提出问题后及时修复并回复 |
| 保持小 PR | 单个 PR 控制在 500 行以内 |
| 响应评论 | 对每条审查评论都需回复（已修复/已讨论/已拒绝并说明原因） |
| 不自行合并 | 未经审查人 Approve 不得自行合并 |

### 3.3 审查人职责

| 职责 | 说明 |
|------|------|
| 及时响应 | 在规定时限内完成审查 |
| 全面审查 | 按 6 大维度逐项检查，不遗漏 |
| 准确分级 | 正确标注 P0-P3 优先级 |
| 提供方案 | 不仅指出问题，还要给出修改建议和代码示例 |
| 客观公正 | 基于代码质量评价，不掺杂个人偏好（P3 除外） |
| 确认修复 | 开发者修复后，审查人需确认问题已解决 |
| 尊重开发者 | 评论对事不对人，语气专业友善 |

**审查范围边界：**

| 审查范围 | 包含 | 不包含 |
|----------|------|--------|
| 代码正确性 | 逻辑错误、边界条件、空指针 | 业务需求正确性（由需求方负责） |
| 代码质量 | 内存安全、类型安全、性能 | 个人风格偏好（P3 级别） |
| 代码规范 | 命名、格式、注释 | 代码美学主观评价 |
| 安全性 | 注入、溢出、权限 | 安全架构设计（由架构师负责） |

### 3.4 维护者职责

| 职责 | 说明 |
|------|------|
| 合并把关 | 确认所有 P0 已修复后才合并 |
| 问题跟踪 | 将 P2/P3 问题创建为 Issue 跟踪 |
| 质量看板 | 定期更新代码质量趋势 |
| 流程优化 | 根据度量数据优化审查流程 |
| 冲突仲裁 | 审查人与开发者意见不一致时仲裁 |

### 3.5 职责边界与冲突处理

**冲突处理流程：**

```
审查人与开发者意见不一致
         │
         ▼
    ┌─────────┐
    │ 友好讨论 │ ──▶ 达成共识 → 修改或保持
    └────┬────┘
         │ 未达成共识
         ▼
    ┌─────────┐
    │ 维护者仲裁│ ──▶ 做出决定
    └────┬────┘
         │ 仍不服
         ▼
    ┌─────────┐
    │ 架构师终裁│ ──▶ 最终决定
    └─────────┘
```

**原则：**
- 技术争论基于数据和标准，不基于个人偏好
- 引用 C++ Core Guidelines、CERT 等权威标准
- P0/P1 问题不可妥协，必须修复
- P2/P3 问题可由开发者决定是否采纳

### 3.6 沟通方式与反馈规范

**评论语气规范：**

| 场景 | 推荐表达 | 不推荐表达 |
|------|----------|------------|
| 指出问题 | "这里使用 sprintf 存在缓冲区溢出风险" | "你怎么还在用 sprintf？" |
| 提出建议 | "建议使用 snprintf 替代，可以限制写入长度" | "这应该用 snprintf" |
| 询问意图 | "这里的逻辑是想处理什么情况？" | "这段代码没有意义" |
| 表达赞同 | "这个 RAII 封装做得很好" | "👍" (仅表情无说明) |
| 请求修改 | "请将 sprintf 改为 snprintf，原因如下..." | "改掉" |

**反馈响应时限：**

| 角色 | 响应时限 |
|------|----------|
| 审查人回复开发者提问 | 1 个工作日内 |
| 开发者修复审查问题 | 2 个工作日内 |
| 维护者合并审查通过的 PR | 1 个工作日内 |

---

## 第四章 审查检查清单

### 4.1 通用检查清单

> **每次 PR 必查项** — 审查人逐项确认

```
□ [P0] 编译无 warning (/W4 /WX)
□ [P0] 无 C 风格强制转换 (type)value
□ [P0] 无裸 new/delete（使用智能指针）
□ [P0] 无 sprintf（使用 snprintf 或 std::format）
□ [P0] 所有指针参数检查 nullptr
□ [P0] 无缓冲区溢出风险（sscanf %s 有限长）
□ [P0] 无命令注入风险（_popen/system 输入已验证）
□ [P1] 所有 Windows API 返回值已检查
□ [P1] 无 malloc/free（使用 new/delete 或容器）
□ [P1] 错误路径资源已释放（RAII 保证）
□ [P1] 函数不超过 100 行
□ [P1] 无硬编码魔法数字（使用命名常量）
□ [P2] 无不必要的字符串拷贝
□ [P2] 新增函数有注释说明
□ [P2] 命名符合规范
□ [P2] include 顺序正确
```

### 4.2 内存安全专项

```
□ [P0] 所有 new 配对 delete，且异常路径也安全
□ [P0] 数组访问无越界风险（边界检查）
□ [P0] memcpy/memmove 目标缓冲区大小足够
□ [P1] 所有 HANDLE/HDC/HBITMAP 正确关闭/释放
□ [P1] ReadProcessMemory/WriteProcessMemory 返回值已检查
□ [P1] OpenProcess 返回的 Handle 已 CloseHandle
□ [P1] GlobalAlloc/GlobalLock 返回值已检查
□ [P1] CreateFile/CreateMutex 返回的 Handle 已关闭
□ [P1] TLS 中 new DmState() 有对应清理（DLL_THREAD_DETACH）
□ [P2] 使用 RAII wrapper 管理所有资源
□ [P2] 考虑使用 std::vector 替代手动数组管理
```

### 4.3 类型安全与 64 位兼容专项

```
□ [P0] 无 (long)hwnd 或 (long)pointer 截断
□ [P0] 无 (int)pointer 截断
□ [P0] 使用 static_cast/reinterpret_cast 替代 C 风格转换
□ [P1] 指针存储使用 intptr_t/uintptr_t 而非 long/int
□ [P1] sizeof 检查而非硬编码大小
□ [P1] 格式化字符串使用 %p/%zd 而非 %d/%ld（指针/大小）
□ [P1] 使用 constexpr 替代 #define 定义常量
□ [P2] 使用 enum class 替代裸枚举和魔法数字
□ [P2] 数值类型转换使用 static_cast 显式标注
□ [P2] 添加 static_assert 验证关键类型大小假设
```

### 4.4 安全专项

```
□ [P0] 用户输入不直接传入 _popen/system/CreateProcess
□ [P0] sscanf 的 %s 格式符有长度限制 (%Ns)
□ [P0] 不使用 _CRT_SECURE_NO_WARNINGS 全局禁用警告
□ [P0] 文件路径检查路径穿越 (../)
□ [P1] 进程操作验证目标进程合法性
□ [P1] 内存读写验证地址范围合法性
□ [P1] 网络请求验证 URL 合法性
□ [P1] 文件操作检查文件大小防止 DoS
□ [P2] 敏感数据不硬编码在源码中
□ [P2] 使用安全版本 CRT 函数 (strcpy_s, sprintf_s)
```

### 4.5 线程安全专项

```
□ [P1] 共享数据访问有锁保护
□ [P1] 无死锁风险（锁顺序一致）
□ [P1] CRITICAL_SECTION 使用 RAII 包装（std::lock_guard）
□ [P1] thread_local 数据正确初始化和清理
□ [P2] 无 TOCTOU (Time-of-Check to Time-of-Use) 竞态
□ [P2] 锁粒度尽可能小（不在持锁时执行 I/O）
□ [P2] 使用 std::call_once 保证一次性初始化
□ [P3] 考虑使用无锁数据结构替代细粒度锁
```

### 4.6 性能专项

```
□ [P1] 无逐像素 GetDC/ReleaseDC（使用批量截屏）
□ [P1] 循环内无不必要的内存分配
□ [P2] 无不必要的字符串拷贝（使用 string_view 或引用）
□ [P2] 找图/找色算法有提前退出机制
□ [P2] 大数据操作考虑多线程
□ [P2] 字符串拼接使用 reserve 预分配
□ [P3] 热点函数考虑 SIMD 加速
□ [P3] 考虑缓存友好型数据布局
```

### 4.7 现代 C++ 专项

```
□ [P1] 使用 RAII 包装所有资源（Handle/DC/Bitmap）
□ [P2] 使用 string_view 替代 const char* + strlen
□ [P2] 使用 [[nodiscard]] 标注不可忽略的返回值
□ [P2] 使用 enum class 替代裸枚举
□ [P2] 使用 nullptr 替代 NULL/0
□ [P2] 使用 using 替代 typedef
□ [P3] 使用 std::optional 表达可能失败的返回值
□ [P3] 使用 std::format 替代 sprintf (C++20)
□ [P3] 使用 constexpr 函数替代模板元编程
```

### 4.8 可维护性专项

```
□ [P1] 单文件不超过 1000 行
□ [P1] 单函数不超过 100 行
□ [P1] 函数单一职责（一个函数只做一件事）
□ [P2] 使用命名空间隔离内部符号
□ [P2] 桩函数需统一标记（DM_STUB 宏）
□ [P2] 公共 API 有文档注释（参数/返回值/错误码）
□ [P2] 文件头有模块说明
□ [P3] 避免过深的嵌套（超过 3 层考虑提取子函数）
□ [P3] 避免过长的参数列表（超过 5 个考虑使用结构体）
```

---

## 第五章 度量与持续改进

### 5.1 核心度量指标

#### 5.1.1 审查过程指标

| 指标 | 定义 | 目标值 | 计算方式 |
|------|------|--------|----------|
| **审查覆盖率** | 经过审查的代码变更占总变更的比例 | ≥ 95% | 审查通过行数 / 总变更行数 × 100% |
| **平均审查周期** | 从提交 PR 到合并的平均时间 | ≤ 2 个工作日 | Σ(合并时间 - 提交时间) / PR 数量 |
| **审查轮次** | 平均每个 PR 的审查-修改轮次 | ≤ 2 轮 | Σ(审查轮次) / PR 数量 |
| **审查人响应时间** | 审查人首次响应的平均时间 | ≤ 4 小时 | Σ(首次响应时间 - 提交时间) / PR 数量 |
| **PR 规模分布** | 不同规模 PR 的分布比例 | 80% 以上 < 500 行 | 按规模区间统计 |

#### 5.1.2 缺陷发现指标

| 指标 | 定义 | 目标值 | 计算方式 |
|------|------|--------|----------|
| **缺陷发现率** | 审查中发现的问题数 / 每千行代码 | ≥ 5 个/千行 | 发现问题数 / 变更行数 × 1000 |
| **缺陷逃逸率** | 合并后发现的问题 / 总问题数 | ≤ 5% | 合并后问题数 / (审查发现 + 合并后发现) × 100% |
| **P0 缺陷拦截率** | 审查中发现的 P0 / 总 P0 数 | 100% | 审查发现 P0 / (审查发现 P0 + 合并后 P0) × 100% |
| **问题修复率** | 已修复问题 / 已发现问题 | ≥ 90% | 已修复数 / 总发现数 × 100% |

#### 5.1.3 代码质量指标

| 指标 | 定义 | 目标值 | 计算方式 |
|------|------|--------|----------|
| **静态分析问题数** | clang-tidy + cppcheck 发现的问题 | 持续下降 | 工具输出统计 |
| **编译警告数** | /W4 下的 warning 数量 | 0 | 编译输出统计 |
| **代码质量评分** | 六大维度加权评分 | ≥ 80 分 (B 级) | 参见 5.2 节 |
| **技术债务** | P2/P3 问题积压数 | 持续下降 | Issue 跟踪统计 |

### 5.2 代码质量评分体系

#### 5.2.1 评分维度与权重

| 维度 | 权重 | 满分 | 评分标准 |
|------|------|------|----------|
| 内存安全 | 25% | 25 | 无泄漏(10) + 无越界(10) + RAII覆盖率(5) |
| 类型安全 | 15% | 15 | 无C风格转换(5) + 64位兼容(5) + 类型安全枚举(5) |
| 安全性 | 20% | 20 | 无注入(8) + 无溢出(7) + 输入验证(5) |
| 性能 | 15% | 15 | 无冗余操作(5) + 算法效率(5) + 资源使用(5) |
| 可维护性 | 15% | 15 | 文件大小(4) + 函数大小(4) + 文档(4) + 命名(3) |
| 现代 C++ | 10% | 10 | RAII使用(3) + 智能指针(3) + 现代特性(4) |

#### 5.2.2 评级标准

| 总分 | 评级 | 说明 | 合并决策 |
|------|------|------|----------|
| 90 - 100 | **A (优秀)** | 可作为参考实现 | 直接合并 |
| 80 - 89 | **B (良好)** | 可合并，少量改进 | 合并，P2 记录跟踪 |
| 70 - 79 | **C (合格)** | 可合并，需跟踪改进 | 合并，P1 限期修复 |
| 60 - 69 | **D (需改进)** | 需修复后合并 | 修复 P0/P1 后合并 |
| < 60 | **F (不合格)** | 必须重大重构 | 拒绝合并 |

#### 5.2.3 评分计算示例

```
模块: 图色 (dm.cpp 中的图色相关函数)

内存安全: 18/25  (有 sprintf 溢出风险 -5, malloc/free 未用 RAII -2)
类型安全: 13/15  (有少量 static_cast 缺失 -2)
安全性:   12/20  (sprintf 未限长 -5, 未验证输入 -3)
性能:      8/15  (逐像素 GetDC -5, 循环内分配 -2)
可维护性: 10/15  (函数过长 -3, 缺少注释 -2)
现代 C++:  6/10  (未用 string_view -2, 未用 [[nodiscard]] -2)
────────────────
总分:     67/100 → D 级 (需改进)
```

### 5.3 持续改进机制

#### 5.3.1 定期审查节奏

| 频率 | 活动 | 参与人 | 产出 |
|------|------|--------|------|
| 每周 | 审查度量数据回顾 | 维护者 | 度量周报 |
| 每两周 | 审查流程改进讨论 | 全体 | 流程更新 |
| 每月 | 全量代码质量审查 | 审查人 + 架构师 | 月度审查报告 |
| 每季度 | 审查标准修订 | 维护者 + 架构师 | 标准更新 |
| 每半年 | 审查流程全面回顾 | 全体 | 流程优化方案 |

#### 5.3.2 改进闭环

```
    ┌──────────┐
    │ 收集度量  │
    └────┬─────┘
         ▼
    ┌──────────┐
    │ 分析数据  │ ──▶ 识别趋势和问题
    └────┬─────┘
         ▼
    ┌──────────┐
    │ 制定方案  │ ──▶ 针对性改进措施
    └────┬─────┘
         ▼
    ┌──────────┐
    │ 实施改进  │ ──▶ 更新标准/流程/工具
    └────┬─────┘
         ▼
    ┌──────────┐
    │ 验证效果  │ ──▶ 对比改进前后度量
    └────┬─────┘
         │
         └────────▶ 回到"收集度量" (PDCA 循环)
```

#### 5.3.3 改进触发条件

| 触发条件 | 阈值 | 改进动作 |
|----------|------|----------|
| P0 缺陷逃逸率 > 0% | 任何 P0 逃逸 | 立即启动专项检查 |
| 缺陷逃逸率 > 10% | 连续 2 个迭代 | 审查标准修订 |
| 平均审查周期 > 3 天 | 连续 2 周 | 流程优化（拆分 PR/增加审查人） |
| 代码质量评分 < 70 | 模块级 | 专项重构计划 |
| 静态分析问题增加 | 环比增加 > 10% | 加强预检门禁 |

### 5.4 度量报告模板

#### 月度代码审查度量报告

```markdown
# 代码审查月度报告

## 报告期间: 2026年 X 月

### 一、审查活动概览

| 指标 | 本月 | 上月 | 趋势 |
|------|------|------|------|
| PR 总数 | X | X | ↑/↓ |
| 审查覆盖率 | X% | X% | ↑/↓ |
| 平均审查周期 | X 天 | X 天 | ↑/↓ |
| 平均审查轮次 | X 轮 | X 轮 | ↑/↓ |

### 二、缺陷发现统计

| 严重性 | 本月发现 | 合并后逃逸 | 逃逸率 |
|--------|----------|------------|--------|
| P0 | X | X | X% |
| P1 | X | X | X% |
| P2 | X | X | X% |
| P3 | X | X | X% |
| 合计 | X | X | X% |

### 三、各模块质量评分

| 模块 | 内存安全 | 类型安全 | 安全 | 性能 | 可维护 | 现代 C++ | 总分 | 评级 |
|------|---------|---------|------|------|--------|---------|------|------|
| 基本设置 | XX | XX | XX | XX | XX | XX | XX | X |
| 窗口 | XX | XX | XX | XX | XX | XX | XX | X |
| 键鼠 | XX | XX | XX | XX | XX | XX | XX | X |
| 图色 | XX | XX | XX | XX | XX | XX | XX | X |
| 内存 | XX | XX | XX | XX | XX | XX | XX | X |
| ... | | | | | | | | |

### 四、问题趋势分析

[分析本月发现的主要问题类型、分布和趋势]

### 五、改进建议

[基于度量数据提出的改进建议]

### 六、下月计划

[下月的审查重点和改进计划]
```

---

## 附录

### 附录 A: 审查报告模板

```markdown
# C++ Code Review Report

## 项目概览
- 评审范围: dm_hook/dm.cpp (全量/增量)
- 评审模式: [全量/增量/PR]
- 评审时间: YYYY-MM-DD
- 评审者: [姓名]

## 问题汇总

| 严重性 | 类型 | 数量 |
|--------|------|------|
| 🔴 P0 | Blocker | X |
| 🟡 P1 | Critical | X |
| 🔵 P2 | Suggestion | X |
| 💭 P3 | Nit | X |

## 模块评分

| 模块 | 内存安全 | 类型安全 | 安全 | 性能 | 可维护性 | 现代 C++ | 总分 | 评级 |
|------|---------|---------|------|------|---------|---------|------|------|
| ... | XX | XX | XX | XX | XX | XX | XX | X |

## 详细问题列表

### P0 问题

#### 1. [问题标题]
- **文件**: dm.cpp:行号
- **函数**: 函数名
- **问题**: [描述]
- **影响**: [影响说明]
- **建议**: [修改建议 + 代码示例]
- **参考**: [C++ Core Guidelines / CERT]

### P1 问题
(同上格式)

## 修复优先级
1. [P0] ...
2. [P0] ...
3. [P1] ...

## 总体评价
[总结性评价 + 改进建议]
```

### 附录 B: 开发者自查快速清单

> **提交代码前，确认以下全部为 ✅：**

```
═══════════════════════════════════════════
  dm_hook 开发者自查清单 (v2.0)
═══════════════════════════════════════════

【编译】
□ 编译通过且无 warning (/W4)
□ 静态分析通过 (clang-tidy + cppcheck)
□ 代码格式化通过 (clang-format)

【内存安全】
□ 无新增 sprintf → 改用 snprintf
□ 无新增裸 new/delete → 改用 unique_ptr
□ 所有指针参数检查 nullptr
□ 所有 Windows API 返回值已检查
□ GlobalLock 返回值检查 NULL
□ 异常路径资源已释放 (RAII)

【类型安全】
□ 无 C 风格转换 (type)value
□ 无指针截断 (long)hwnd
□ 使用 constexpr 替代 #define
□ 使用 intptr_t 存储指针值

【安全】
□ 用户输入不直接传入 _popen/system
□ sscanf 的 %s 有长度限制
□ 文件路径检查路径穿越

【性能】
□ 无逐像素 GetDC/ReleaseDC
□ 循环内无内存分配
□ 无不必要的字符串拷贝

【规范】
□ 函数不超过 100 行
□ 无硬编码魔法数字
□ 新增函数有注释说明
□ 命名符合约定 (dm_ 前缀, snake_case)
□ PR 描述完整 (变更说明 + 测试方式)

═══════════════════════════════════════════
  全部 ✅ 后提交 PR
═══════════════════════════════════════════
```

### 附录 C: 自动化工具配置

#### .clang-tidy

```yaml
Checks: >
  -*,
  bugprone-*,
  cert-*,
  cppcoreguidelines-*,
  modernize-*,
  performance-*,
  readability-*,
  -modernize-use-trailing-return-type,
  -readability-magic-numbers,
  -cppcoreguidelines-avoid-magic-numbers,
  -readability-identifier-length

WarningsAsErrors: >
  bugprone-*,
  cert-*,
  cppcoreguidelines-pro-bounds-*,
  modernize-use-nullptr

HeaderFilterRegex: 'dm\.h$'
```

#### .clang-format

```yaml
BasedOnStyle: LLVM
IndentWidth: 4
ColumnLimit: 120
AlignConsecutiveAssignments: true
AlignConsecutiveDeclarations: true
PointerAlignment: Left
BreakBeforeBraces: Attach
AllowShortFunctionsOnASingleLine: Inline
AllowShortIfStatementsOnASingleLine: false
```

#### CMake 编译选项

```cmake
if(MSVC)
    target_compile_options(dm_hook PRIVATE
        /W4           # 高级别警告
        /WX           # 警告视为错误
        /permissive-  # 严格标准合规
        /Zc:__cplusplus  # 正确设置 __cplusplus 宏
    )
endif()

# 启用 ASan (Debug)
option(ENABLE_ASAN "Enable AddressSanitizer" OFF)
if(ENABLE_ASAN)
    target_compile_options(dm_hook PRIVATE -fsanitize=address)
    target_link_options(dm_hook PRIVATE -fsanitize=address)
endif()
```

#### Cppcheck 配置

```bash
cppcheck --enable=all --suppress=missingIncludeSystem \
         --error-exitcode=1 \
         --inline-suppr \
         -DDM_EXPORTS \
         dm.cpp dm.h dllmain.cpp
```

### 附录 D: 已知问题清单

> 基于 2026-08-11 代码库分析

#### P0 — 阻塞项（必须修复）

| # | 文件:行 | 问题 | 影响 |
|---|---------|------|------|
| 1 | dm.cpp:159,166 等 19 处 | `sprintf` 无长度限制 | 🔴 缓冲区溢出风险 |
| 2 | dm.cpp:209,219 | `malloc`/`free` 管理 ImageCodecInfo | 🔴 异常不安全，泄漏风险 |
| 3 | dm.cpp:84 | `new DmState()` 裸指针，TLS 泄漏风险 | 🔴 如果 TlsSetValue 失败则泄漏 |

#### P1 — 关键项（强烈建议修复）

| # | 文件:行 | 问题 | 影响 |
|---|---------|------|------|
| 4 | dm.cpp:171-175 | `GetPixelColor` 逐像素 GetDC/ReleaseDC | 🟡 性能极差 |
| 5 | dm.cpp 全文 | 3366 行单体文件，应按模块拆分 | 🟡 可维护性差 |
| 6 | dm.cpp:104,110 | `strncpy` 使用可接受但建议封装 | 🟡 代码重复 |
| 7 | dm.h:136 | `CRITICAL_SECTION criSection` 声明但审查需确认使用 | 🟡 线程安全验证 |

#### P2 — 建议项

| # | 文件 | 问题 |
|---|------|------|
| 8 | dm.cpp | 无命名空间，全局符号污染 |
| 9 | dm.cpp | 大量桩函数无统一标记，难以区分实现状态 |
| 10 | dm.h | 函数参数无文档注释 |
| 11 | dm.cpp | findPic 逐像素比较，无 SIMD/多线程优化 |

### 附录 E: 术语表

| 术语 | 全称 | 含义 |
|------|------|------|
| RAII | Resource Acquisition Is Initialization | 资源获取即初始化 — C++ 资源管理范式 |
| UB | Undefined Behavior | 未定义行为 — 标准未定义的代码行为 |
| TOCTOU | Time-of-Check to Time-of-Use | 检查-使用竞态 — 并发安全漏洞模式 |
| SIMD | Single Instruction Multiple Data | 单指令多数据 — 并行加速技术 |
| ASan | AddressSanitizer | 地址消毒器 — 运行时内存错误检测 |
| UBSan | UndefinedBehaviorSanitizer | 未定义行为消毒器 |
| PR | Pull Request | 拉取请求 — 代码合并提案 |
| MR | Merge Request | 合并请求 — 同 PR (GitLab 术语) |
| TLS | Thread Local Storage | 线程本地存储 |
| PDCA | Plan-Do-Check-Act | 计划-执行-检查-行动 — 持续改进循环 |
| CRT | C Runtime Library | C 运行时库 |

---

> **文档维护**: 本文档随代码库演进而更新。每次审查标准修订需经过维护者和架构师审核。  
> **最近更新**: 2026-08-11  
> **版本历史**: v1.0 (2026-08-10) 初版 | v2.0 (2026-08-11) 全面扩展审查标准、流程、角色、清单、度量五大板块
