# 2026-08-13 工作日志

## dm_com.cpp 代码审查与标准更新

### 完成内容
1. **全面代码审查 dm_com.cpp** (1339行): 发现 17 个问题 (P0×2, P1×7, P2×5, P3×3)
   - P0-1: Invoke() 中 `new VARIANT[]` 裸分配，异常路径内存泄漏
   - P0-2: VARIANT 浅拷贝（裸赋值）违反 COM 所有权语义
   - P1: 42处C风格函数指针转换、线性查找O(n)、tolower有符号char UB等
   - 总评分: 65/100 (D级，需改进)
2. **更新 CODE_REVIEW_STANDARDS.md 至 v3.0**:
   - 新增维度 G: COM 安全 (权重7%)，含8条规则 (G1-G8)
   - 新增 §4.9 COM 安全专项检查清单 (12项)
   - 更新评分权重 (7维度重新分配)
   - 已知问题清单从11项扩展到25项 (含dm_com.cpp发现)
   - 适用范围明确纳入 dm_com.cpp / dm_com.h
3. **生成审查报告**: CODE_REVIEW_dm_com.md
4. **可视化**: 质量评分雷达图 + 三阶段审查流程图

### 关键决策
- 评分权重从6维度调整为7维度，COM安全占7%，其余维度相应缩减
- P0问题集中在内存安全维度，建议优先修复
- 42处C风格转换可批量替换为reinterpret_cast

## dm_com.cpp 全量修复

### 完成内容
修复了审查发现的 17 个问题中的 15 个（2 个记录为 backlog），编译验证通过。

**P0 修复 (2/2):**
- P0-1: `new VARIANT[]` → `std::vector<VARIANT>` + RAII 析构守卫
- P0-2: VARIANT 浅拷贝 → `VariantCopy` / `VariantClear`

**P1 修复 (6/7):**
- P1-1: 43 处 C 风格函数指针转换 → `reinterpret_cast`（Python 脚本批量替换）
- P1-2: O(n) 线性查找 → `unordered_map` + `call_once` 线程安全懒初始化
- P1-3: `tolower(*p)` UB → `tolower(static_cast<unsigned char>(*p))`
- P1-4: 参数提取函数添加 `assert(args != nullptr)`
- P1-5: `goto error` → `ScopedHKey` RAII 结构体
- P1-7: 6 个 H_ 函数去重 → `SetResultCoord` / `SetResultCoord4`
- P1-6: DispatchByPattern 拆分 → 记录为 backlog（42 case 结构保留）

**P2 修复 (4/5):**
- P2-1: 匿名命名空间包裹所有内部符号
- P2-2: `sprintf_s` → `snprintf`（15 处）
- P2-3: FuncPattern 枚举添加命名规则文档注释
- P2-4: `wcscpy_s`/`wcscat_s` → `std::wstring` 拼接
- P2-5: ENTRY 宏编译期安全 → 记录为 backlog

**P3 修复 (3/3):**
- P3-2: `WCHAR progIdW[]` → `const WCHAR*` 指针
- P3-3: 辅助函数全部添加 `///` 文档注释
- 所有 C 风格数值转换 → `static_cast`

### 修复后评分
- 修复前: 65/100 (D 级)
- 修复后: 90/100 (A- 级)
- 编译验证: 通过 (cmake --build build --config Release, exit 0)

## dm_com.cpp COM 分发表补全（40 个缺失条目）

### 背景
结合 API_REFERENCE.md 审查时发现：`DISPID` 枚举定义了 417 个函数，但 `g_dispTable[]` 仅注册 377 个，40 个函数（窗口操作 2、内存操作 23、文字识别 4、Foobar 5、AI 6）无分发表条目，**无法通过 COM 调用**（pre-existing bug，非 Phase 2 引入）。

### 完成内容
1. **补齐 40 个分发表条目**（dm_com.cpp）：
   - 10 个用现有 `ENTRY_*` 标准模式直接映射：`findWindowEx`(P1L1L1S)、`readDataAddrToBin`(P1L2L)、`writeIntAddr`/`writeDataFromBin`/`writeDataAddrFromBin`(L4L)、`writeFloatAddr`(P1L1L1F)、`writeStringAddr`(P1L2L1S)、`writeDataAddr`(P1L1L1S)、`foobarSetSave`(P1L1S1L)、`foobarSetTrans`(P1L2L)
   - 30 个用 `ENTRY_SPECIAL` + 新建 `H_*` 处理器（涉及输出参数/指针+字符串/float/double/超参函数）：`enumWindowSuper`、`readFloatAddr`、`readDoubleAddr`、`readStringAddr`、`readDataAddr`、`findIntEx`、`findFloat`/`findFloatEx`、`findDouble`/`findDoubleEx`、`findString`/`findStringEx`、`findData`/`findDataEx`、`writeDouble`/`writeDoubleAddr`、`virtualQueryEx`、`setDictMem`、`findStrWithFont`/`E`/`Ex`、`loadAiMemory`、`aiFindPicMem`/`Ex`、`aiYoloDetectObjectsToDataBmp`/`ToFile`、`aiYoloSetModelMemory`、`foobarDrawText`/`Rect`/`Line`
2. **构建验证**：通过 VS DevShell PowerShell 模块（规避 cmd.exe 限制）注入 VS2022 环境 + Ninja 编译，exit 0，产物 `build/dm_hook.dll` (225280B, 15:38)
   - 校验脚本确认：DISPID 417/417 全覆盖，0 缺失、0 重复，49 个 SPECIAL 处理器全部有定义
3. **API_REFERENCE.md 更新**：
   - "COM 尚未支持的函数"章节 → "COM 支持状态（完整覆盖）"，说明 40 函数已于 v1.4 全部补齐（标准模式 10 + SPECIAL 30）
   - 文档版本 1.3 → 1.4（页眉 + 页脚）

### 关键决策
- 输出参数函数在 COM 中统一经 `SetResult*` 辅助函数返回拼接字符串（`x|y` 等），保持与原版大漠 COM 接口一致
- 构建在无 cmd.exe 权限的沙箱中，改用 VS DevShell 模块 + 日志重定向方案（build_com_ps.ps1）；该脚本可复用

### 备注 / 待办
- 未做运行时 COM 注册烟雾测试（需提权改注册表，有副作用）；建议手动 `regsvr32 dm_hook.dll` 后跑 test_com.py 验证
- Phase 2 两 backlog 仍保留：P1-6 (DispatchByPattern 42-case 拆分)、P2-5 (ENTRY 宏编译期类型安全)
